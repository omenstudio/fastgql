models:
  Todo:
    model: github.com/omenstudio/fastgql/example/todo.Todo
  ID:
    model: # override the default id marshaller to use ints
      - github.com/omenstudio/fastgql/graphql.IntID
      - github.com/omenstudio/fastgql/graphql.ID
